package com.txw.designpattern.command;

/**
 * 命令模式
 * 使用场景
 * 需要抽象出待执行的动作，然后以参数的形式提供出来--类似于过程设计中的回到机制，而命令模式正是毁掉机智的一个面向对象的替代品。
 * 在不同的时刻指定、排列和执行请求。一个命令对象可以有与初始请求无关的生存期。
 * 需要支持取消操作。
 * 支持修改日志功能，这样当系统崩溃的时候，这些修改可以被重做一遍。
 * 需要支持事务操作。
 * 优点：
 *
 * 缺点：
 *
 * @author txw
 * @date 2017/11/8.
 */
public class Command {
}
