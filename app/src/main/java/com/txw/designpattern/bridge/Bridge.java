package com.txw.designpattern.bridge;

/**
 * 桥接模式 定义 将抽象部分与实现部分分离，使他们都可以独立地进行变化。
 * 场景
 * 1. 如果一个系统需要在构建的抽象化角色和具体化角色之间增加更多灵活性，避免在两个层次之间建立静态的继承联系。
 * 2. 对于那些不希望使用继承或因为多层继承导致系统类的数量急剧增加的系统
 * 3. 一个类存在连个独立变化的维度，且两个维度都需要进行扩展。
 *
 * @author txw
 * @date 2017/11/8.
 */
public class Bridge {
}
